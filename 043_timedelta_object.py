from datetime import datetime, timedelta
project_duration = timedelta(weeks=2, days=4)
print(project_duration)

daily_hours = timedelta(hours=8, minutes=30, seconds=0)
print(daily_hours)

duration_for_independance_day = datetime(2025, 8, 15) - datetime(2025, 8, 11)
print(duration_for_independance_day)

